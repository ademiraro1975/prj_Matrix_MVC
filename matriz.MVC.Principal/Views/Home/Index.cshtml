@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!-- INÍCIO DO CONTEÚDO -->
<div class="cn-cont col-sm-12 col-md-12 col-lg-10 col-xl-10 col-xxl-10">
    <!-- INÍCIO DO CONTAINER -->
    <div class="container">

        <!-- INÍCIO DOS BREADCRUMBS -->
        <!-- FIM DO BREADCRUMBS -->

        <h1 class="mb-3 text-center">Principal</h1>
        <!-- INÍCIO DA DIV - TABELA -->
        <div class="cn-div-table mb-3">
            <!-- INÍCIO DA TABELA -->
            <!-- FIM DA TABELA -->
        </div>
        <br />
        <br />
        <div class="row container">
            <div class="col-4">
                <!-- INÍCIO DA DIV - TABELA -->
                <div id="nav-menu">
                    <!-- INÍCIO DA LISTA -->
                    <ul>
                        <!-- INÍCIO DOS LINKS -->
                        <li class="cn-link">
                            <a class="d-flex flex-row" asp-area="" asp-controller="Pizza" asp-action="Index">
                                <!-- INÍCIO DA DIV - ÍCONE -->
                                <div class="cn-link-icon">
                                    <img src="~/images/geral/pizzaII.jpg" alt="pizza" width="28px" height="28px">
                                </div>
                                <!-- FIM DA DIV ÍCONE -->
                                <h6 class="d-flex flex-column container">
                                    <span>Pizza</span>
                                </h6>
                            </a>
                        </li>
                        <!-- FIM DOS LINKS -->
                    </ul>
                    <!-- FIM DA LISTA -->
                </div>
                <!-- FIM DA DIV - TABELA -->
            </div>
            <div class="col-4">
                <!-- INÍCIO DA DIV - TABELA -->
                <div id="nav-menu">
                    <!-- INÍCIO DA LISTA -->
                    <ul>
                        <!-- INÍCIO DOS LINKS -->
                        <li class="cn-link">
                            <a class="d-flex flex-row" asp-area="" asp-controller="Refrigerante" asp-action="Index">
                                <!-- INÍCIO DA DIV - ÍCONE -->
                                <div class="cn-link-icon">
                                    <img src="~/images/geral/refrigerante.png" alt="refrigerante" width="28px" height="28px">
                                </div>
                                <!-- FIM DA DIV ÍCONE -->
                                <h6 class="d-flex flex-column container">
                                    <span>Refrigerante</span>
                                </h6>
                            </a>
                        </li>
                        <!-- FIM DOS LINKS -->
                    </ul>
                    <!-- FIM DA LISTA -->
                </div>
                <!-- FIM DA DIV - TABELA -->
            </div>
            <div class="col-4">
                <!-- INÍCIO DA DIV - TABELA -->
                <div id="nav-menu">
                    <!-- INÍCIO DA LISTA -->
                    <ul>
                        <!-- INÍCIO DOS LINKS -->
                        <li class="cn-link">
                            <a class="d-flex flex-row" asp-area="" asp-controller="Ingrediente" asp-action="Index">
                                <!-- INÍCIO DA DIV - ÍCONE -->
                                <div class="cn-link-icon">
                                    <img src="~/images/geral/ingrediente.png" alt="ingrediente" width="28px" height="28px">
                                </div>
                                <!-- FIM DA DIV ÍCONE -->
                                <h6 class="d-flex flex-column container">
                                    <span>Ingrediente</span>
                                </h6>
                            </a>
                        </li>
                        <!-- FIM DOS LINKS -->
                    </ul>
                    <!-- FIM DA LISTA -->
                </div>
                <!-- FIM DA DIV - TABELA -->
            </div>
        </div>
        <br />
        <br />
        <div class="row container">
            <div class="col-4">
                <!-- INÍCIO DA DIV - TABELA -->
                <div id="nav-menu">
                    <!-- INÍCIO DA LISTA -->
                    <ul>
                        <!-- INÍCIO DOS LINKS -->
                        <li class="cn-link">
                            <a class="d-flex flex-row" asp-area="" asp-controller="Tamanho" asp-action="Index">
                                <!-- INÍCIO DA DIV - ÍCONE -->
                                <div class="cn-link-icon">
                                    <img src="~/images/geral/tamanho.png" alt="tamanho" width="28px" height="28px">
                                </div>
                                <!-- FIM DA DIV ÍCONE -->
                                <h6 class="d-flex flex-column container">
                                    <span>Tamanho</span>
                                </h6>
                            </a>
                        </li>
                        <!-- FIM DOS LINKS -->
                    </ul>
                    <!-- FIM DA LISTA -->
                </div>
                <!-- FIM DA DIV - TABELA -->
            </div>
            <div class="col-4">
                <!-- INÍCIO DA DIV - TABELA -->
                <div id="nav-menu">
                    <!-- INÍCIO DA LISTA -->
                    <ul>
                        <!-- INÍCIO DOS LINKS -->
                        <li class="cn-link">
                            <a class="d-flex flex-row" asp-area="" asp-controller="Tipo" asp-action="Index">
                                <!-- INÍCIO DA DIV - ÍCONE -->
                                <div class="cn-link-icon">
                                    <img src="~/images/geral/tipo.png" alt="tipo" width="28px" height="28px">
                                </div>
                                <!-- FIM DA DIV ÍCONE -->
                                <h6 class="d-flex flex-column container">
                                    <span>Tipo</span>
                                </h6>
                            </a>
                        </li>
                        <!-- FIM DOS LINKS -->
                    </ul>
                    <!-- FIM DA LISTA -->
                </div>
                <!-- FIM DA DIV - TABELA -->
            </div>
            <div class="col-4">
                <!-- INÍCIO DA DIV - TABELA -->
                <div id="nav-menu">
                    <!-- INÍCIO DA LISTA -->
                    <ul>
                        <!-- INÍCIO DOS LINKS -->
                        <li class="cn-link">
                            <a class="d-flex flex-row" asp-area="" asp-controller="TabelaPreco" asp-action="Index">
                                <!-- INÍCIO DA DIV - ÍCONE -->
                                <div class="cn-link-icon">
                                    <img src="~/images/geral/tabelapreco.png" alt="tabelapreco" width="28px" height="28px">
                                </div>
                                <!-- FIM DA DIV ÍCONE -->
                                <h6 class="d-flex flex-column container">
                                    <span>Tabela de Preço</span>
                                </h6>
                            </a>
                        </li>
                        <!-- FIM DOS LINKS -->
                    </ul>
                    <!-- FIM DA LISTA -->
                </div>
                <!-- FIM DA DIV - TABELA -->
            </div>
        </div>
        <br />
        <br />
    </div>
    <!-- FIM DO CONTAINER -->
</div>
<!-- FIM DO CONTEÚDO -->